# Copy this file to web-app/.env.local and fill in your values.
# The web-app's Next.js server loads this automatically.

# ── Required ────────────────────────────────────────────────────────────────

# Anthropic API key — used by the narrative engine to generate credit decisions.
# Get one at: https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=sk-ant-...

# ── PayU Integration ─────────────────────────────────────────────────────────

# "mock" (default) — realistic simulated EMI plans, no credentials needed.
# "live"           — real PayU sandbox: generates a signed payment session and
#                    redirects the browser to test.payu.in for checkout.
PAYU_MODE=mock

# Required when PAYU_MODE=live.
# Sign up at https://developer.payu.in → Dashboard → Test Credentials
# to get your sandbox PAYU_KEY and PAYU_SALT (≠ production values).
# PAYU_KEY=your_payu_sandbox_merchant_key
# PAYU_SALT=your_payu_sandbox_merchant_salt

# Optional: override the PayU success/failure redirect URL (defaults to Vercel).
# Set this to http://localhost:3000/payment/callback when testing locally.
# PAYU_RETURN_URL=http://localhost:3000/payment/callback
