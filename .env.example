# Copy this file to web-app/.env.local and fill in your values.
# The web-app's Next.js server loads this automatically.

# ── Required ────────────────────────────────────────────────────────────────

# Anthropic API key — used by the narrative engine to generate credit decisions.
# Get one at: https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=sk-ant-...

# ── PayU Integration ─────────────────────────────────────────────────────────

# "mock" (default) — realistic simulated EMI plans, no credentials needed.
# "live"           — real PayU LazyPay sandbox API calls.
PAYU_MODE=mock

# Only required when PAYU_MODE=live (PayU sandbox credentials):
# PAYU_KEY=your_payu_sandbox_merchant_key
# PAYU_SALT=your_payu_sandbox_merchant_salt
